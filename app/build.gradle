plugins {
    alias(libs.plugins.androidApplication)
}

android {
    namespace = "com.openipc.pixelpilot"
    compileSdk = 34

    defaultConfig {
        applicationId = "com.openipc.pixelpilot"
        minSdk = 26
        targetSdk = 34
        // Bump versionCode AND versionName before every commit so the side-loaded
        // APK is easy to identify on-device (Settings -> Apps -> PixelPilot).
        versionCode = 2
        versionName = "0.24.0"

        ndk {
            abiFilters.add("arm64-v8a")
            abiFilters.add("armeabi-v7a")
        }
    }

    signingConfigs {
        create("release") {
            storeFile = file("../fpv.jks")
            storePassword = "release"
            keyAlias = "fpv"
            keyPassword = "release"
        }
    }

    buildTypes {
        release {
            minifyEnabled = true
            shrinkResources = true
            signingConfig = signingConfigs.release
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'),'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    buildFeatures {
        viewBinding = true
    }
    packaging {
        jniLibs {
            excludes += ["lib/armeabi-v7a/libmediapipe_tasks_vision_jni.so"]
            useLegacyPackaging = true
        }
    }
    buildToolsVersion '35.0.0'
    ndkVersion '26.1.10909125'
}

dependencies {
    implementation(project(":app:mavlink"))
    implementation(project(":app:videonative"))

    implementation(libs.appcompat)
    implementation(libs.material)
    implementation(libs.mediapipe.tasks.vision)
}
